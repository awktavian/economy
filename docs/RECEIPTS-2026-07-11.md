# Kernel Axiom Receipts — economy

Kernel axiom receipts for economy's principal theorems: raw `#print
axioms` output, captured directly from the compiler.

Generated: 2026-07-11T15:34:28Z
Toolchain: `leanprover/lean4:v4.31.0`
Command: `lake build Economy && lake env lean _receipts_ax.lean` (scratch
file, not committed) under `~/.claude/hooks/lean-build-lock.sh`.
HEAD at generation: `df6ded9 chore: checkpoint first-party Lean work`

## Method

Selected from `CLAUDE.md`'s "seven-module spine" table, which explicitly
names each module's load-bearing theorems. Picked the strongest 6 of the 13
named candidates: the macro composition keystone (`acemoglu_macro_bound`),
both `ScenarioSpace` dominance theorems (the lifting result plus the
welfare/GDP-divergence result), and all three failure-mode-module keystones
(`JobSwapping`, `RecessionShock`, `CapexBubble`). Exact fully-qualified
names verified by tracing `namespace .. end` boundaries in each source file
— two candidates (`recession_expected_loss`, `bubble_pop_ghost_gdp_loss`)
sit directly under `namespace Economy` rather than the inner
`RecessionParams`/`CapexParams` namespace their theorem list appears next
to, since the inner namespace closes (`end RecessionParams` / `end
CapexParams`) before those declarations.

## Receipts

```
'Economy.TaskEconomy.acemoglu_macro_bound' depends on axioms: [propext, Classical.choice, Quot.sound]
'Economy.gdp_dominance_from_intelligence_dominance' depends on axioms: [propext, Classical.choice, Quot.sound]
'Economy.welfare_can_diverge_under_any_trajectory' depends on axioms: [propext, Classical.choice, Quot.sound]
'Economy.jobSwap_cobbDouglas_zero_Lnon_limit' depends on axioms: [propext, Classical.choice, Quot.sound]
'Economy.recession_expected_loss' depends on axioms: [propext, Classical.choice, Quot.sound]
'Economy.bubble_pop_ghost_gdp_loss' depends on axioms: [propext, Classical.choice, Quot.sound]
```

| # | Declaration | Statement (informal, one line) | Axioms | Verdict |
|---|---|---|---|---|
| 1 | `Economy.TaskEconomy.acemoglu_macro_bound` | Discrete Hulten corollary: if productivity changes only on an automated task set S with per-task log-gain ≤ c, aggregate log-TFP change ≤ exposureShare(S)·c (Acemoglu w32487) | propext, Classical.choice, Quot.sound | CLEAN |
| 2 | `Economy.gdp_dominance_from_intelligence_dominance` | Pointwise dominance of one intelligence trajectory over another lifts to the same weak ordering on induced log-GDP deviations, for any t ≥ 0 | propext, Classical.choice, Quot.sound | CLEAN |
| 3 | `Economy.welfare_can_diverge_under_any_trajectory` | For every capability trajectory shape, there exist parameters where welfare falls while log-GDP rises (labor-share-collapse witness, trajectory-independent) | propext, Classical.choice, Quot.sound | CLEAN |
| 4 | `Economy.jobSwap_cobbDouglas_zero_Lnon_limit` | As the non-substitutable labor bucket L_non → 0⁺, CES-nest Cobb-Douglas output → 0 regardless of augmented-capital magnitude (the "hard floor" claim) | propext, Classical.choice, Quot.sound | CLEAN |
| 5 | `Economy.recession_expected_loss` | Expected cumulative recession loss unfolds to λ·duration·shockSize·T — a named DEFINITIONAL EXPOSURE (closed by `rfl`; the file's own docstring flags this as a modeling-input restatement, not a derived Poisson-process expectation) | propext, Classical.choice, Quot.sound | CLEAN (definitional) |
| 6 | `Economy.bubble_pop_ghost_gdp_loss` | The Ghost-GDP loss when a capex bubble pops equals (1-α)·(gK_hyper - gK_maintenance), the algebraic difference between sustainable and unsustainable growth contributions | propext, Classical.choice, Quot.sound | CLEAN |

6/6 CLEAN. No sorryAx, no custom axioms — confirms `CLAUDE.md`'s stated
invariant ("Zero sorry. Zero user-defined axioms.") holds for this sample,
and none of the six leans on `native_decide` (consistent with the repo's
own "no decide/native_decide on real-valued goals" rule, since all six are
real-valued). One honest finding: `Economy.recession_expected_loss` (#5) is
a `rfl`-closed unfolding of a named definition rather than a derived
analytic fact — the source docstring already labels it "DEFINITIONAL
EXPOSURE...not mathematical content," so this is the repo's own documented
design choice, not a new defect; flagged here for transparency since the
audit protocol asks for genuine findings, not just clean bills of health.
Full-corpus 207-theorem coverage was not re-audited by this wave — see
`/tmp/proof-report.json` for the compiler-wide count.
